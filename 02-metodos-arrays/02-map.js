const numeros = [1, 2, 3, 4, 5];

const dobrado = numeros.map((numeros) => numeros*2);

console.log(dobrado)

let nomes = ["brenda", "carlos", "felipe", ]

let mensagem = nomes.map((nome) => `ola, ${nome}`)

console.log(mensagem)